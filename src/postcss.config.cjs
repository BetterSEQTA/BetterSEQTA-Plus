// This is the PostCSS configuration file for the project
module.exports = {
  plugins: {
    // TailwindCSS plugin to enable Tailwind CSS functionality
    tailwindcss: {},

    // Autoprefixer plugin to automatically add vendor prefixes to CSS for cross-browser compatibility
    autoprefixer: {},
  },
};
